const num1 = 5;
const num2 = 10;

function addValues(){
    console.log(`the sum is : ${num1+num2}`)
}

addValues()

//Adding a module will automatically execute the functions in it even though
//the function IF IT IS CALLED in it